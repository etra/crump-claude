{
  "description": "Validate crump CLI availability",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-crump.sh"
          }
        ]
      }
    ]
  }
}
